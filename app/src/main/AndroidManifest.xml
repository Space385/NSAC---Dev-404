<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.encylo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".act_america"></activity>
        <activity android:name=".atlas" />
        <activity android:name=".aqua" />
        <activity android:name=".adeos" />
        <activity android:name=".atom" />
        <activity android:name=".ats" />
        <activity android:name=".aquarius" />
        <activity android:name=".air_moss" />
        <activity android:name=".menu" />
        <activity android:name=".Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>